python -m venv venv
source venv/bin/activate
pip install -r requirements.txt

-- Test Commands --
Read all resumes in the resumes folder
Find resumes mentioning Python
Create a summary file for resumes/resume_john_doe.txt
Search resumes/resume_jane_smith.txt for SQL